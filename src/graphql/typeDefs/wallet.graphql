scalar JSON

type GetWalletResponse {
  success: Boolean
  message: String
  balance: Float
}

type GetWalletTransactionResponse {
  success: Boolean
  message: String
  transactions: [JSON]
}

type WalletResponse {
  success: Boolean
  message: String
}

# Add/update methods when needed
enum CashInMethodType {
  GCASH
  PAYMAYA
  OVER_THE_COUNTER
  DEBIT_CARD
  CREDIT_CARD
}

# Main
type Query {
  getWalletBalance: GetWalletResponse
  getWalletTransactions: GetWalletTransactionResponse
}

type Mutation {
  walletCashIn(amount: Float, method: CashInMethodType): WalletResponse 
  walletPayment(loanId: Int): WalletResponse
} 