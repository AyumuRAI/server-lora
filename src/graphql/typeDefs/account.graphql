type ResponseMPIN {
  success: Boolean
  message: String
}

# Main
type Query {
  sendMPIN(phone: String): ResponseMPIN
  verifyMPIN(phone: String, code: String): ResponseMPIN
}

type Mutation {
  _empty: String!
}
